import 'package:flutter/material.dart';

class Favorite_Page extends StatelessWidget {
  const Favorite_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
body: Center(
  child:Text("Add Recipies"),
),
    );
  }
}
